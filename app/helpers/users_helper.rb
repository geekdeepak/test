module UsersHelper
# --- kept here for future implimentation ---
#  def gravatar_image(user)
#    image_tag(gravatar_url_for(h(user.email)), :alt => h(user.name))
#  end
#
#  def gravatar_url_for(email)
#    'http://www.gravatar.com/avatar/' + Digest::MD5.hexdigest(email) + '?s=80'
#  end
end
